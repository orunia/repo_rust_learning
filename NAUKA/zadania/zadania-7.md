1. Utwórz nowy pakiet o nazwie `firma` za pomocą Cargo, który będzie zawierał zarówno crate biblioteczny (`lib.rs`), jak i crate binarny (`main.rs`).
2. W pliku `lib.rs` zdefiniuj moduł `dzial_it` zawierający prywatną funkcję `napraw_serwer` oraz publiczną funkcję `zresetuj_haslo`. Na poziomie głównym (crate root) napisz publiczną funkcję `zglos_problem`, która wywołuje `zresetuj_haslo` używając ścieżki bezwzględnej.
3. W module `dzial_hr` stwórz publiczną strukturę `Pracownik` posiadającą publiczne pole `imie` (`String`) oraz prywatne pole `wynagrodzenie` (`u32`). Utwórz dla niej publiczną funkcję powiązaną `nowy(imie: &str) -> Pracownik`, która tworzy pracownika z domyślnym wynagrodzeniem.
4. Zdefiniuj funkcję `wyslij_raport()` na poziomie głównym pliku `lib.rs`. Utwórz zagnieżdżony moduł `dzial_sprzedazy::raportowanie` i wewnątrz niego funkcję `zakoncz_miesiac()`. Użyj słowa kluczowego `super` (odpowiednią liczbę razy), aby z wnętrza `zakoncz_miesiac()` wywołać funkcję `wyslij_raport()`.
5. W pliku `main.rs` użyj słowa kluczowego `use`, aby zaimportować funkcję `zresetuj_haslo` z modułu `dzial_it` biblioteki `firma`, a następnie wywołaj ją w funkcji `main()` bez podawania pełnej ścieżki.
6. Zaimportuj do pliku `main.rs` typ `std::fmt::Result` oraz typ `std::io::Result`. Użyj słowa kluczowego `as`, aby nadać jednemu z nich nową nazwę i uniknąć konfliktu, a następnie stwórz dwie puste funkcje zwracające te różne typy.
7. Wyodrębnij moduł `dzial_it` z pliku `lib.rs` do osobnego pliku `dzial_it.rs`, pozostawiając w `lib.rs` jedynie deklarację modułu wymaganą do poprawnej kompilacji.
8. Wyodrębnij zagnieżdżony moduł `raportowanie` (znajdujący się wewnątrz modułu `dzial_sprzedazy`) do własnego pliku, tworząc odpowiedni folder zgodnie z polecaną strukturą nazewnictwa w Rust (bez użycia plików `mod.rs`).